const express = require("express");
const router = express.Router();

router.get("/", (req, res) => {
  res.send("MinhasEconomias2 no ar");
});

module.exports = router;

//Este arquivo foi criado por último. Descobrir os proximos passos e dar andamento.